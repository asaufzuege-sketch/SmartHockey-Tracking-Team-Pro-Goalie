{
  "name": "smarthockey-tracking-team-pro",
  "version": "2.0.0",
  "description": "SmartHockey Team Tracker Pro - Professional Hockey Team Management",
  "main": "index.html",
  "scripts": {
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "cordova-plugin-purchase": "^13.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
